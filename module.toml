name = "blvm-mesh"
version = "0.1.0"
description = "Commons Mesh networking module for blvm-node"
author = "Bitcoin Commons Team"
entry_point = "blvm-mesh"

capabilities = [
    "read_blockchain",
    "subscribe_events",
]

# CI fills url/sha256 on release — root `blvm-mesh` binary; see `modules/*/module.toml` for workspace members.
[downloads]
x86_64-linux   = { url = "", sha256 = "" }
aarch64-linux  = { url = "", sha256 = "" }
x86_64-windows = { url = "", sha256 = "" }

